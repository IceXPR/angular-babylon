{
  "name": "angular-babylon-server",
  "version": "1.0.0",
  "description": "Boilerplate for angular babylon environment",
  "main": "server.ts",
  "scripts": {
    "start": "cd ../webapp/ && npm run build && cd ../server && ts-node app.ts",
    "just-server": "ts-node app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julio Colon",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.27.2",
    "express": "^4.18.1",
    "ts-node": "^10.7.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}
